# /brief Context: dispatch-paste-prompt

## Entry Mode
freeform

## Upstream Path
none

## Topic Slug
dispatch-paste-prompt

## Visibility
Public

## Artifact Decision
produce

## Phase
4

## Notes

Invoked in-chain by `/scope` (sentinel present in
`wip/scope_dispatch-paste-prompt_state.md`, `invoking_child: brief`,
`rationale: fresh-chain`).

A prior `/explore` round produced substantial framing at
`wip/explore_dispatch-paste-prompt_{scope,findings,decisions}.md` and six
research files under `wip/research/explore_dispatch-paste-prompt_r1_lead-*.md`.
That framing is ephemeral -- `wip/` is cleaned before the PR merges -- which is
precisely why the artifact decision is `produce` rather than fold-into-PRD: the
problem, outcome, journeys, and scope boundary need a durable home, and a
standalone BRIEF is it.
