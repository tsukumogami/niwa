# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
version: 2

builds:
  - id: niwa
    main: ./cmd/niwa
    binary: niwa-{{ .Os }}-{{ .Arch }}
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    flags:
      - -trimpath
      - -buildvcs=false
    ldflags:
      - -s -w -X github.com/tsukumogami/niwa/internal/buildinfo.version={{.Version}} -X github.com/tsukumogami/niwa/internal/buildinfo.commit={{.Commit}} -X github.com/tsukumogami/niwa/internal/buildinfo.date={{.Date}}
    mod_timestamp: "{{ .CommitTimestamp }}"
    no_unique_dist_dir: true

archives:
  - format: binary
    name_template: "{{ .Binary }}"

checksum:
  name_template: checksums.txt
  algorithm: sha256

changelog:
  disable: true

release:
  github:
    owner: tsukumogami
    name: niwa
  draft: true
  make_latest: true
  prerelease: auto

snapshot:
  version_template: "{{ incpatch .Version }}-next"
